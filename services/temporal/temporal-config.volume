[Unit]
Description=Temporal Dynamic Configuration Volume

[Volume]
# Mount the local config/temporal directory for dynamic configuration
Driver=local
Device=/opt/temporal/config/temporal
Type=bind
Options=bind

[Install]
WantedBy=multi-user.target